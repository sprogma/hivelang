
(i64 a; i64 b)pow(i64? res)
{
    decl i64 t;
    t <- 1;
    while b > 0
    {
        t <- t * a;
        b <- b - 1;
    }
    res <- t;
}

(i64 a; i64 b)worker(i64? y)
{
    ~ (a + 1, b + 1)pow(t1);
    ~ (a, b)pow(t2);
    y <- ?t1 + ?t2;
}

