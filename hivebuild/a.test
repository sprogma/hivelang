

(i64 a)fib(i64? res)
{
    match (a)
    {
        0 { res <- 0; }
        1 { res <- 1; }
        default t : a > 1 {
            ~ (t - 1)fib(res1);
            ~ (t - 2)fib(res2);
            res <- ?res1 + ?res2;
        }
    }
}
